import { Meta, Story, Source, Canvas, Controls } from "@storybook/addon-docs";
import * as BoxStories from "../Box/Box.stories";

<Meta of={BoxStories} />

# Box

`Box` is a flexible layout component that supports rendering different semantic HTML tags such as `div`, `section`, `article`, `aside`, `nav`, `header`, and `footer`. It allows you to customize styling via Tailwind CSS classes passed in the `className` prop.

Use `Box` as a foundational building block for creating more complex layout components while maintaining semantic HTML structure and consistent styling.

## Features

- Supports rendering any semantic HTML tag via the `element` prop
- Accepts Tailwind CSS utility classes through `className`
- Lightweight and unopinionated—great for composition
- Inherits native HTML attributes (`id`, `aria-*`, `data-*`, etc.)
- Compatible with all layout components (`Grid`, `Flex`, `Stack`, etc.)

## Usage Guidelines

- Choose the appropriate semantic tag with the `element` prop for better accessibility and SEO.
- Use `Box` to wrap content blocks where you want flexible layout and styling control.
- Apply Tailwind CSS classes through `className` for spacing, background, borders, etc.
- Combine with other layout components like `Container`, `Section`, or `Grid` for more complex layouts.

## Do

- Use the `element` prop to render semantic HTML tags
- Use Tailwind classes via `className` for layout/styling
- Wrap layout sections or blocks with `Box`

## Don’t

- Hardcode `<div>` everywhere
- Mix inline styles with Tailwind
- Nest headers or navs incorrectly inside other semantic tags

## Props

<Controls of={BoxStories.Default} />

# Component Relationship Diagram

### Examples

## Variants

These are common variations of the `Box` component using different HTML tags via the `element` prop.

### Default (div)

<Canvas of={BoxStories.Default} />

### Section

<Canvas of={BoxStories.Section} />

### Article

<Canvas of={BoxStories.Article} />

### Aside

<Canvas of={BoxStories.Aside} />

### Nav

<Canvas of={BoxStories.Nav} />

### Header

<Canvas of={BoxStories.Header} />

### Footer

<Canvas of={BoxStories.Footer} />

### Source Code

<Source of={BoxStories.Footer} language="tsx" />
