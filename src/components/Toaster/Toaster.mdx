import { Meta, Canvas, Source } from '@storybook/blocks';
import * as ToasterStories from './Toaster.stories';

<Meta of={ToasterStories} />

# Toaster

The `Toaster` component provides toast notifications with theme support.

## Basic Usage

<Canvas of={ToasterStories.Default} />
<Source of={ToasterStories.Default} />